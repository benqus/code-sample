export function main() {
  // Object.entries(process.env).forEach(([k, v]: [ string, string? ]): void => {
  //   if (k.startsWith('npm_package_config')) {
  //     console.log(k, v);
  //   }
  // });
  return 'Hello A!';
}
